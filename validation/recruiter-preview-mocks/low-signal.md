# Mock — low-signal repo (junior tutorial-shaped)

Input: create-react-app defaults, no customization, no deployment, no
README content beyond CRA boilerplate.

Expected preview output:

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃   RECRUITER PREVIEW — 55 second simulated scan              ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

── 0:00–0:30  Above-the-fold ────────────────────────────────
  Title:        Getting Started with Create React App
  Pitch:        (missing — recruiter cannot tell what this is)
  Status:       absent
  Demo link:    absent
  Tech badges:  absent

  Recruiter first impression:
    "Looks like internal tool — but no clear pitch. Skim and decide."
    (In practice: closed within 10 seconds.)

── 0:30–2:00  Scroll + section scan ─────────────────────────
  Architecture diagram: no — depth invisible
  Deployment evidence:  not visible
  AI transparency:      absent (2026 risk: undisclosed-AI assumption)

── 2:00+  Deep-dive (only top ~20% of recruiters reach here) ─
  ADRs:    missing
  Runbook: missing
  Tests:   missing
```

Virality check: this is the most *painful* mock. The engineer sees
how thin the repo reads. The fix list (replace CRA boilerplate
README, add deploy evidence, add a pitch line) is short and
actionable — high motivation to share once fixed.
